{
    "scripts": {
        "dev": "concurrently npm:dev:*",
        "dev:server": "npm run dev --prefix ./server",
        "dev:app": ""
    },
    "devDependencies": {
        "concurrently": "^8.2.0"
    }
}
